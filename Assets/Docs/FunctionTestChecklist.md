# 功能测试表

## 测试环境
- 日期：`2026-04-21`
- 构建版本：`D:\unityhub\project\build\Ky_UAV\Ky_UAV.exe`
- 自动化日志：`Library/Logs/packaged-functional-run.log`
- 运行日志：`C:\Users\KangYun\AppData\LocalLow\DefaultCompany\Ky_UAV\Player.log`
- 当前导出会话：`C:\Users\KangYun\AppData\LocalLow\DefaultCompany\Ky_UAV\ExperimentResults\20260421\20260421_171949_runtime`

## 结果

| 编号 | 功能项 | 验证方式 | 结果 | 备注 |
| --- | --- | --- | --- | --- |
| F-01 | 主场景启动 | 启动打包版 | 通过 | 直接进入 `MainScene`，运行时面板自动创建，系统状态正常进入就绪。 |
| F-02 | 开始仿真 | `F5` | 通过 | `Player.log` 记录 `状态切换: Running`，并开始分配任务与路径规划。 |
| F-03 | 暂停/继续 | `F6` 后 `F5` | 通过 | `Player.log` 记录 `Paused -> Running`，仿真恢复后能继续完成任务。 |
| F-04 | 重置仿真 | `F7` | 通过 | 10 轮稳定性测试均可重置并回到空闲态，无阻塞性错误。 |
| F-05 | 重建机群 | `F8` | 通过 | 默认 `4` 架无人机可重建并继续仿真；“修改数量后再重建”仍需人工复核。 |
| F-06 | 调度切换 | 运行时实验中心切换调度算法 | 待人工复核 | 本轮自动化未点击面板切换 `EvenSplit / GreedyNearest / PriorityGreedy`。 |
| F-07 | 规划切换 | 运行时实验中心切换规划算法 | 待人工复核 | 本轮自动化未点击面板切换 `StraightLine / AStar / RRT`。 |
| F-08 | 任务点新增 | 运行时面板新增任务点 | 待人工复核 | 打包版自动化未覆盖鼠标点地新增流程。 |
| F-09 | 任务点导入 | 自动默认导入 | 部分通过 | `Player.log` 记录默认 `4` 个任务点自动导入成功；手动点击“导入任务”按钮仍需人工复核。 |
| F-10 | 起飞点编辑 | 新增/移动/删除起飞点 | 待人工复核 | 本轮自动化未覆盖起飞点编辑交互。 |
| F-11 | 导出 CSV | `Ctrl+Shift+C` | 通过 | 已生成 `simulation_experiment_records.csv`。 |
| F-12 | 导出 JSON | 运行时导出 JSON | 待人工复核 | 本轮自动化未拿到 JSON 产物，需手动点击面板再次确认。 |
| F-13 | 批量实验 | 运行时启动批量实验 | 待人工复核 | 本轮自动化未拿到 `session_manifest.json` 与 `session_summary.csv`，需手动点击面板验证。 |
| F-14 | 自定义导出目录 | 运行时修改导出目录 | 待人工复核 | 本轮自动化未覆盖目录选择交互。 |
| F-15 | 相机切换 | `3` 切到 `2D俯视` | 部分通过 | `2D俯视` 已验证可用；`总览 / 跟随 / 下一架无人机` 仍需人工复核。 |
| F-16 | 2D 轨迹检查 | `2D俯视` + 轨迹投影 | 通过 | 轨迹与规划线可在 `XZ` 平面直接观察，建筑不会遮挡路径判断。 |
| F-17 | 建筑重叠/穿越告警 | `2D俯视` 下运行默认任务集 | 部分通过 | 本轮默认任务集 `建筑告警 = 0`，未出现穿楼；显式触发告警的专项用例仍需人工复核。 |
| F-18 | 退出程序 | `F10` | 通过 | 打包版可正常退出，不再需要强制结束进程。 |

## 结论
- 当前打包版主流程可用：启动、运行、暂停、继续、重置、重建、CSV 导出、`2D俯视` 和退出均已通过。
- `kinematic rigidbody` 速度写入警告已修复，本轮打包版日志未出现相关警告。
- 仍需人工补测的项目集中在“面板点击交互”和“批量实验/JSON 导出”两类。
